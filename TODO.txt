## high priority ##
# check if adding bombcell classification to sorting analyzer worked!
- implement raw data quality metrics
- implement option to split_by for multiple shanks/regions

## regular ##
- write check if all data exists for certain probe and skip if overwrite is false in run_processing function
	- prevents separate checks for all steps
- visualization
	- visualize pre-processing once done?/save visualization as .png?
	- save visualizations to given path
	- change postprocessing/figures to figures?, not used currently
- skip the ones that already have preprocessing data for calculating needed storage space in run preprocessing
- function after copying that deletes all empty folders in local dir
- add option to set use_local_raw_copy = True (in preprocessing), if yes then copy raw recording to SSD/local first, read from that, delete again after preprocessing
	- check if this is faster first
- upgrade check_folder_strucutre function so it takes list for parent and subfolders
- pip install pynvml in environment, create proper environemnt file with torch --index url
- implement manual curation (in phy)
	- # -> copy to seperate folder, create seperate environment for phy, new script
- implement LFP pipeline
	-  def LFP_extraction()
		-> large chunks & large margins (30-60s chunks, use default margins (5s))
- option to read pre-processed recording from final drive in postprocessing?
- after curation -> visualization & QC -> result collection -> NWB packaging
- make lfp function usable at other places in pipeline, maybe all annotations = true when loading raw is enough so you later have access to stream info etc.

## mid-term/before next experiments ##
- make possible to launch manual outside channel detection if delete_manually_slected called from pipeline and no data found
	- use subset = [recording_identifier] to limit manual detection to this recording
- add experiment to NeuropixelsData class and use it when building path?

## low priority ##
- add automatic pre-selection to manual detection plot if enabled -> suggestion based on detect bad channels
	- all outside channels have very regular low/high voltage variations (see in manual channel selection, window = 10s) -> detect these to set boundary
- show previously selected channel/boundary line if overwrite existing data = true and data exists in manual outside detector
- code better function that automatically detects outside channels
- enable scrolling through time in manual channel selection with ipywidgets backend
- create option to write new file instead
	- maybe existing_files: ["overwrite", "keep", "skip_step"]
	- multiple times sorting & repeat_sorting = true -> new folder called sorting_n, can reuse matlab function?
		- if spike_sorting exists go up to folder.parent, make new subfolder based on how many sorters of that name in parent, prefix, reassign output_folders.xyz
		- or make this a function in OutputPaths class (e.g.: output_folders.new_iteration() if exist already)
- raw spike sorting vs saving via sorted_spikes.save(folder=output_folder.sorting_local, format="numpy_folder") and deleting raw spike sorting?
- implement comparison of 2 sorters
	- comparison = si_comparison.compare_two_sorters(sorting1, sorting2) # also possible to compare more via .comapre_multiple_sorters
	- # when comparing 2 sorters you can extract basesorting -> units agreed on
		- sorting_agreed = comparison.get_agreement_sorting(minimum_agreement_count)
	- compare spike sorters for IC
- make available as pip
	- alternative name if taken: signal processing, universal graphs and analysis routines commonly applied in neuropixels experiments (sugarcane)
- use get_probe_name from master_thesis/utilities.py in recording identifier
- separate class for processing functions so things like output etc. can be handled more easily?